const CONSTANTS_GO = "up here, too";

module.exports = function (mysqlConnection) {
    return {
        /*
         * Here's is where some data will go regarding each function for
         * mailing. It would be useful to specify what the inputs/outputs are
         * and what the function does with them.
         */
        funcName: function(data, done) {
            // if an error is returned, send it with
            // done(err, null);
            // else, return data with
            // done(null, data);
        },
        /*
         * ...
         */
        funcName2: function(data1, data2, data3) {
            // ...
        },
        /*
         * ...
         */
        },
    };
}
